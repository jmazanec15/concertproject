var express   = require('express'),
    Base      = express.Router(),
    fs        = require('fs'),
    User      = require('../models/user'),
    bcrypt    = require('bcrypt');







module.exports = Base;
